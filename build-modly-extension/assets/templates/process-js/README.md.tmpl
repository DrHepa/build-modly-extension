# @@EXTENSION_NAME@@ — Modly Process Extension

@@DESCRIPTION@@

## Status

REPLACE_ME: State the highest completed validation tier and exact tested platforms.

## Features

- Modly node: `@@NODE_ID@@` (`@@INPUT_TYPE@@` → `@@OUTPUT_TYPE@@`)
- Isolated Node worker
- Structured progress and log reporting

## Installation

1. Open Modly and go to **Extensions/Models**.
2. Select **Install from GitHub**.
3. Paste `@@SOURCE_URL@@`.
4. Modly installs production dependencies from `package.json`.
5. Add **@@NODE_NAME@@** to a workflow.

## Usage

REPLACE_ME: Describe the input, workflow wiring and shortest successful run.

## Parameters

REPLACE_ME: Document every parameter and runtime default.

## Outputs

REPLACE_ME: Document the returned file/text and workspace location.

## Requirements and compatibility

REPLACE_ME: List Node/native dependencies and verified platforms.

## Limitations

REPLACE_ME: State unsupported inputs, size limits and native-addon constraints.

## Troubleshooting

- **Dependency error:** verify that all runtime packages are under `dependencies` and that `package-lock.json` is current.
- **Input rejected:** verify the preceding node emits `@@INPUT_TYPE@@`.

REPLACE_ME: Add process-specific recovery steps.

## Upstream and credits

- Modly extension integration: @@AUTHOR@@
- Extension repository: [@@SOURCE_URL@@](@@SOURCE_URL@@)
- Upstream project: [@@UPSTREAM_URL@@](@@UPSTREAM_URL@@), pinned to `REPLACE_ME_WITH_COMMIT`
- Host application: [Modly](https://github.com/lightningpixel/modly) by Lightning Pixel

## License

The extension wrapper is licensed under @@WRAPPER_LICENSE@@. Upstream code is licensed under @@UPSTREAM_LICENSE@@. See `THIRD_PARTY_NOTICES.md`; upstream and dependency terms remain in force.
